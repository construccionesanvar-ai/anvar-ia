{
  "name": "anvar-ia",
  "version": "2.0.0",
  "private": true,
  "description": "ANVAR TECH · IA & Automatización — sitio de automatización e inteligencia operacional para empresas.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node scripts/build.mjs && node scripts/check.mjs",
    "dev": "npm run build && python -m http.server 8123 --directory public",
    "deploy": "vercel --prod"
  }
}
